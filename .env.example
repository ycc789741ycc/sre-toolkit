# Copy this file to .env and adjust values as needed.
# .env is gitignored - it's meant to hold the values specific to your setup.

# Remote host to target for vscode-server-cleanup.sh
HOST=172.16.1.13

# SSH user on the remote host (leave empty to use your local SSH config / default user)
SSH_USER=
